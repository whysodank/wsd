from apps.common.apps import CommonConfig


class FeedbackConfig(CommonConfig):
    name = "apps.feedback"
    # TODO: this breaks the app but we don't use this right now anyway
    # This is not essential for the app to work
