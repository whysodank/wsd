#! /usr/bin/env bash

# This is an example of a custom precommit hook, any and all custom hooks we'll need
# will be under /scripts/precommit/

echo precommit is installed and running. This hook should always pass.
